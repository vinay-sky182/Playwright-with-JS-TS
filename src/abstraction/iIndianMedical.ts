export interface IndianMedical {

    // only method decleration - no method body
    // we can not create the object/ instance of interface.

    gestro(): void;
    ent(): void;
    emergency(): void;
}